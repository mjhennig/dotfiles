# Disable the screen help/startdp messages
startup_message off

# Disable the visual bell (a blinking screen, very annoying)
vbell off

# A very simple bottom line: Tab names, nothing else.
caption always "%{= kw}%-w%{= BW}%n %t%{-}%+w"

